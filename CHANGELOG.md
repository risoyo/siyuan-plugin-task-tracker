# 更新日志

本文档记录本项目对外发布版本的主要变更，版本号遵循 `X.Y.Z` 规则：
- `X`：大版本，表示明显不兼容变更或重要阶段里程碑；
- `Y`：小版本，表示向后兼容的新功能或明显增强；
- `Z`：缺陷修复，表示不改变功能面的修复和小幅改进。

## [3.5.0] - 2026-05-22

### 发布说明
- 发布 `3.5.0`，新增移动端插件入口与任务追踪页面兼容，首版聚焦“任务查看 + 树形展开折叠 + 点击标题进入任务编辑”。

### 新增与优化
- `plugin.json` frontends 新增 `mobile` 与 `browser-mobile`，插件可在移动端加载并展示入口。
- 移动端复用现有 `TaskDock` 任务树逻辑，不复制业务逻辑，支持 `全部 / 重点 / 今日` 与父子任务展开折叠。
- 移动端任务标题点击进入任务编辑弹窗（`TaskDialog`），不直接打开笔记。
- 移动端打开笔记时改用 `openMobileFileById`，匹配思源移动端打开文档方式。
- 为 `TaskDock` 与 `TaskDialog` 补充最小移动端样式适配，保证窄屏可滚动、不溢出、可点击。

## [3.4.2] - 2026-05-18

### 发布说明
- 发布 `3.4.2`，继续收敛 `3.4.1` 后续暴露的任务概要托管回归，重点修正父任务在多次新建子任务、关闭后重开、父子关系变更时的摘要稳定性。

### 缺陷修复
- 将任务概要表格后的 `父任务 / 子任务 / 任务描述 / ---` 正式切换为基于思源块模型的 sibling block 托管，不再把标签行拼入表格块 markdown。
- 重构摘要表格后的托管区识别规则：
  - 允许托管区内部存在空白 block；
  - 只有遇到真正的下一个正文块或标题块才结束扫描；
  - 避免被空白块或历史残留块提前打断。
- 将标签区同步策略改为“识别整段托管区 -> 清理旧标签/空白/重复 divider -> 按固定顺序整体重建”，保证幂等性。
- 修复父任务在以下场景中摘要重复、顺序漂移、关闭重开后子任务信息丢失的问题：
  - 连续新建多个子任务；
  - 从文件树或任务控制面板重新打开父任务；
  - 子任务移到其他父任务下；
  - 子任务移回顶层；
  - 删除子任务。
- 继续强化父子关系恢复链路：重建索引时同时按 `task.id` 和 `docId` 解析 `parentId`，降低重开后父子关系恢复不稳定的概率。

### 兼容性说明
- 本次主要修正 `3.4.1` 的摘要托管回归，不改变任务模型、任务详情受控分区、归档规则或任务控制面板的基础交互。
- 已存在的任务文档在再次触发同步后，会逐步收敛为唯一一组标准标签行。

## [3.4.1] - 2026-05-18

### 发布说明
- 发布 `3.4.1`，作为一次统一兼容修复，集中解决当前任务概要模板托管、父子任务移动路径语义、以及项目字段输入方式三类彼此耦合的问题。

### 缺陷修复
- 正式兼容 `## 任务概要` 下的“表格 + 标签行”摘要模板：
  - 继续托管任务概要表格；
  - 同步托管紧随表格后的 `**父任务**`、`**子任务**`、`**任务描述**` 三行；
  - 父子关系变化、删除子任务、修改任务描述后，父任务与子任务文档摘要都会同步刷新。
- 修复新建父任务后再挂接已有任务为子任务时，偶发误把 hpath 当真实 path 使用的问题：
  - `TaskItem.path` 仅保存真实 `blocks.path`；
  - 创建后若暂时拿不到真实 path，不再写入猜测 hpath；
  - 移动、归档、删除前都会强制实时回读真实路径，拿不到则终止操作，避免错误创建假的“事项库/父任务”结构。
- 恢复任务弹窗中的项目字段为“可选已有项目 + 可输入新项目”的 editable combobox：
  - 新建与编辑任务都可直接输入项目名；
  - Enter 确认、失焦保留、选择“无项目”清空；
  - 最终提交值统一来自真实文本输入框；
  - 父任务字段仍保持选择型。
- `TaskItem.description` 继续作为唯一数据源：
  - 若文档中存在摘要标签行 `**任务描述**`，会同步更新；
  - 若存在独立 `## 任务描述` 分区，也会同步更新；
  - 两种结构同时存在时保持一致。

### 模板与说明更新
- 默认任务模板更新为当前正式支持的任务概要结构，默认包含任务概要表格与父任务/子任务/任务描述标签行。
- 设置页中的模板说明同步更新，明确摘要托管区与任务详情受控分区的职责边界。

### 兼容性说明
- 本次不对整段 `## 任务概要` 做重建式改写，只托管已识别到的摘要表格及其紧随标签行，尽量避免误伤用户手写内容。
- 旧任务文档中的引用块摘要、独立 `## 任务描述` 分区仍保持兼容。

## [3.2.4] - 2026-05-17

### 发布说明
- 发布 `3.2.4`，进一步修正任务控制面板表格页父子任务树形连接线，使层级关系更连贯、更紧凑。

### 样式优化
- 将子任务纵向树线从虚线改为浅色实线，增强父子任务之间的连续性。
- 非最后一个子任务的纵向线填满当前行上下区域，与相邻子任务保持连贯。
- 最后一个子任务的纵向线只延伸到当前项中部，避免多余线条继续向下。
- 子任务连接线保持圆角折线和末端小圆点，减少生硬折角并提升树形结构识别度。
- 压缩子任务行高和上下间距，避免树线导致子任务区域显得松散。

### 兼容性说明
- 本次仅涉及表格树形层级 UI 样式和子任务末项标记，不改变任务数据结构、业务逻辑、筛选规则、状态规则或优先级规则。

## [3.2.3] - 2026-05-17

### 发布说明
- 发布 `3.2.3`，继续优化任务控制面板表格页顶部工具栏与父子任务层级展示，使入口更清晰、表格层级更明确。

### 样式优化
- 右侧工具区保持“页面设置 / 全部任务”顺序，并将两个入口统一为蓝色描边按钮。
- 页面设置按钮改为专用样式，文字右侧显示齿轮图标，与筛选按钮形成一致的视觉权重。
- 全部任务筛选按钮恢复右侧下拉箭头，使用独立按钮样式避免 `b3-button` 默认样式干扰。
- 优化父子任务层级：父任务标题保持加粗，子任务增加更明显缩进、浅灰树形竖线，并降低子任务标题颜色权重。
- 父任务标题右侧显示子任务数量徽标，便于快速识别任务层级规模。

### 兼容性说明
- 本次仅涉及 UI 和交互入口展示调整，不改变任务数据结构、业务逻辑、筛选规则、状态规则或优先级规则。

## [3.2.2-hotfix] - 2026-05-17

### 发布说明
- 发布 `3.2.2-hotfix`，修复任务控制面板右上角“全部任务”筛选按钮的视觉居中与按钮尺寸一致性问题。

### 缺陷修复
- 移除筛选按钮右侧下拉箭头，避免“文字 + 箭头”整体居中导致文字视觉偏左、右侧留白过大的问题。
- 保留筛选按钮文字居中结构，并恢复与“页面设置”按钮一致的高度与边框节奏。
- 筛选按钮仍保留原有点击和筛选下拉逻辑，仅调整展示结构与样式。

### 兼容性说明
- 本次仅涉及 UI 样式和按钮内部展示结构，不改变筛选逻辑、状态修改、优先级修改、新建任务或数据结构。

## [3.2.2] - 2026-05-17

### 发布说明
- 发布 `3.2.2`，针对任务控制面板表格页继续定向修正实际生效的样式选择器，解决筛选按钮留白与表格行高问题。

### 样式优化
- 为右上角“全部任务”筛选按钮增加专用 class，绕开通用 `b3-button` 样式带来的宽度与内边距干扰，使按钮内容自适应且左右留白一致。
- 定向命中真实表格结构 `task-manager-table__row` 与 `task-manager-table__cell`，将表格主体行高控制在更集中的 76px 节奏。
- 将任务标题限制为最多两行展示，避免长标题继续撑高整行。
- 保持状态与优先级 Badge 的紧凑尺寸，并补充 `min-height` 约束，避免控件反向影响行高。
- 收敛表格操作列按钮与图标尺寸，保证操作列垂直居中且不撑高行。

### 兼容性说明
- 本次仍仅涉及 UI 样式和筛选按钮 class 定向标记，不改变业务逻辑、数据结构、筛选逻辑、状态修改或优先级修改行为。

## [3.2.1] - 2026-05-17

### 发布说明
- 发布 `3.2.1`，继续微调任务控制面板表格页的视觉密度与控件对齐，保持清爽、正式的企业中台风格。

### 样式优化
- 修复右上角“全部任务”筛选按钮内部布局，使文字视觉居中并保持左右留白一致。
- 缩小表格内状态与优先级 Badge 的高度、字号、图标和色点尺寸，降低视觉权重。
- 压缩表格行高与单元格上下间距，让任务列表更集中但不拥挤。
- 统一表格主体、次要信息与表头字号层级，提升整体一致性。
- 调整操作列图标垂直居中与行内节奏，避免操作列撑高行高。

### 兼容性说明
- 本次仅涉及 UI 样式优化，不改变业务逻辑、数据结构、筛选逻辑、状态修改或优先级修改行为。

## [3.2.0] - 2026-05-16

### 发布说明
- 发布 `3.2.0`，对"任务追踪"侧边栏进行三轮渐进式视觉重构与紧凑度优化，打造企业中台风格的轻量任务速览面板。

### 功能增强
- **筛选标签页重构**：将"全部/焦点/今日"改为"全部/重点/今日"，重新定义筛选逻辑。
  - 全部：显示所有活跃任务（待处理、进行中、等待中）。
  - 重点：高优先级任务、已逾期任务、3 天内截止或计划结束的任务、进行中且有计划时间的任务。
  - 今日：计划开始、计划结束或截止日期为今天的活跃任务。
- **卡片式布局**：任务以白底卡片形式展示，带圆角与浅色边框，hover 时显示蓝色边框与微阴影。
- **只读状态 Badge**：状态标签改为只读胶囊样式，颜色统一使用 `STATUS_BADGE_CONFIG` 配色体系。
- **人性化日期 Badge**：新增 `formatSidebarDate()` 工具函数，按优先级（截止日期 → 计划结束 → 计划开始）展示日期，自动检测逾期/今天/明天/本周/日期，并以对应胶囊样式呈现。
- **父子任务树形层级**：父任务左侧展开/折叠 chevron 箭头，子任务缩进展示并带 CSS 树形连接线，父任务显示子任务数量徽标。
- **筛选空状态**：每个筛选标签页在无任务时显示独立的空状态画面，包含对应图标、标题与引导文案。
- **紧凑度两轮优化**：
  - 第一轮：移除底部"添加任务"固定栏，头部"打开控制面板"按钮替换为紧凑"+"添加按钮，全局缩小字号与间距。
  - 第二轮：移除任务卡片右侧"..."更多操作按钮，任务标题支持最多 2 行自动换行（`line-clamp: 2`），进一步压缩 Badge、间距、子任务缩进与树形连接线尺寸。
- 主面板筛选按钮内边距修复，左右对称。

### 技术细节
- 新增 `formatSidebarDate()` 到 `src/date.ts`，提供统一的侧边栏日期计算与展示逻辑。
- 侧边栏样式全部限定在 `.task-tracker--dock` / `.task-tracker-dock` 作用域下，不影响主任务控制面板。
- 所有用户数据（标题、项目等）通过 `escapeHtml()` 处理，防止 XSS。
- 移除更多操作下拉菜单渲染与相关事件绑定（`activeMoreMenuTaskId`、`renderMoreMenu`、`handleMoreAction` 等），精简组件状态。
- 保留 `includeAncestors()`、`buildTaskTree()`、`renderChevron()` 等树构建逻辑不变。

### 兼容性说明
- 不影响任务 CRUD 逻辑、数据持久化、主任务控制面板与日历视图。
- 侧边栏筛选仅影响展示，不改变任务状态与属性。

## [3.1.6] - 2026-05-16

### 发布说明
- 发布 `3.1.6`，重构笔记信息区布局：笔记 ID 嵌入来源控件内部，任务描述移至来源右侧形成横向双栏结构。

### 功能增强
- 笔记 ID 输入框嵌入来源 SegmentedControl 所在淡蓝色区域内，选择"笔记"时在右侧直接显示，不再以独立区块出现。
- 去掉"笔记 ID"独立标签文字，输入框 placeholder 改为"填写笔记ID"。
- 任务描述从来源下方移至来源右侧，与来源形成横向并列双栏布局（`1fr 1fr`，间距 15px）。
- "任务描述"标签与"来源"标签顶部对齐，结构更紧凑。

### 兼容性说明
- 不影响任务 CRUD 逻辑、sourceMode 切换逻辑、笔记 ID 校验与保存、任务描述回填行为。

## [3.1.5] - 2026-05-16

### 发布说明
- 发布 `3.1.5`，压缩弹窗纵向间距，修复日期选择框重叠问题，优化字段对齐，使表单更紧凑且字段清晰可辨。

### 缺陷修复
- 压缩弹窗整体纵向间距：内容区 padding 从 32px 缩至 24px，header/footer padding 收紧，分区间距从 24px 缩至 14px，行间距从 20px 缩至 18px，section 内部 gap 从 16px 缩至 6px，section-title margin-bottom 从 16px 缩至 6px。
- 修复时间信息区四列日期选择框重叠问题：移除 `max-width` 限制，`column-gap` 调整为 70px，四列均分全宽，新增响应式断点（≤1200px 两列、≤720px 一列）。
- 项目/父任务行网格从 `1fr 1.5fr` 改为 `1fr 1fr`，与下方状态/优先级行上下对齐。
- 任务标题宽度调整为 `max-width: calc(100% - 25px)`，右侧与父任务选择栏对齐。
- 来源行从 Flex 切换为 CSS Grid（`360px 1fr`），布局更稳定。
- SegmentedControl 选项高度从 34px 提升至 40px，与全局控件规范统一。
- 日期输入框移除 `!important` 强制样式，语义更清晰。

### 兼容性说明
- 不影响任务 CRUD 逻辑、来源模式数据提交流程及状态/优先级交互行为。

## [3.1.4] - 2026-05-16

### 发布说明
- 发布 `3.1.4`，修复 3.1.3 弹窗落地后的交互细节与布局问题（Combobox 关闭行为、字段宽度、滚动、间距、文案）。

### 缺陷修复
- 修复 Combobox 切换按钮再次点击无法关闭菜单的问题，增加关闭状态判断逻辑。
- 时间信息区四列行增加 `max-width: 75%`，字段宽度与状态/优先级 Badge 宽度保持一致。
- 滚动容器 `body-scroll` 改用绝对定位（`position: absolute; top:0; left:0; right:0; bottom:0`），替代 `height:100%` 方案，确保在 Flex 容器中可靠滚动。
- 任务信息区行间距统一为 `gap: 16px`，与分区标题间距保持一致。
- "创建时间（系统记录）"标签简化为"创建时间"。

### 兼容性说明
- 不影响任务 CRUD 逻辑、来源模式数据提交流程及状态/优先级交互行为。

## [3.1.3] - 2026-05-16

### 发布说明
- 发布 `3.1.3`，修复 3.1.2 弹窗落地后的核心可用性问题（选择框不可用、宽度溢出、滚动失效、来源区显隐异常）。

### 缺陷修复
- 修复项目/父任务 Combobox 菜单被 overflow 裁剪导致无法弹出的问题：菜单改用 `position:fixed` + `getBoundingClientRect` 定位，彻底绕过滚动容器裁剪上下文。
- 项目/父任务行改为非等宽布局（1fr 1.5fr），项目栏更窄，两栏之间留白自然，不再超出弹窗边界。
- 时间信息区字段添加专用尺寸类 `input--date`，字号收紧至 12px，内边距同步缩减，四列不再相互挤压覆盖。
- 来源区显隐从 `hidden` 属性改为 `element.style.display` 控制，解除 CSS `display:flex` 覆盖 UA `[hidden]` 的问题。
- 滚动容器 `body-scroll` 改用 `height:100%` 替代 `max-height:100%`，配合 `body` 的 `overflow:hidden` 约束纵向滚动链，头部与底部固定不动。

### 兼容性说明
- 不影响任务 CRUD 逻辑、来源模式数据提交流程及状态/优先级交互行为。

## [3.1.2] - 2026-05-16

### 发布说明
- 发布 `3.1.2`，修复 3.1.1 弹窗落地后的布局与交互问题。

### 缺陷修复
- 修复项目/父任务 Combobox 下拉菜单因 body overflow 裁剪导致无法弹出的问题，将滚动容器内移。
- 项目与父任务两栏间距从 16px 增至 24px，消除视觉穿模感。
- 移除普通新建模式下父任务下方"如需创建子任务，请在此选择父任务"提示文案。
- 时间信息区四列输入框字号从 14px 收紧至 13px，列间距从 16px 缩至 12px，不再相互覆盖。
- 来源区选择"手动创建"时彻底隐藏笔记 ID 输入区域，移除"当前来源"摘要栏。
- 状态/优先级拆分独立标签（"状态"/"优先级"），文字从 14px 调整为 13px，控件密度更克制。
- 新增"任务信息"分区标题，分区标题字号从 12px 提升至 15px，三区层级更清晰。

### 兼容性说明
- 不影响任务 CRUD 逻辑、来源模式数据提交流程及状态/优先级交互行为。

## [3.1.1] - 2026-05-16

### 发布说明
- 发布 `3.1.1`，在 3.1.0 的自定义下拉与弹窗 v3 基础上对布局与交互细节做进一步收敛优化。
- 弹窗改为分区布局（基础信息 / 时间信息 / 笔记信息），整体更紧凑清晰，适合政府 / 金融科技任务管理系统。
- 项目、父任务选择改为自定义 Combobox + Popover 风格，与状态/优先级菜单统一视觉语言。
- 来源改为分段控件（Segmented Control），布局更规整。
- 创建时间改为只读系统记录字段，不再作为可编辑输入项。

### 功能增强
- 弹窗宽度 1080px，内容左右内边距 32px，区域之间以浅色虚线分隔。
- 任务标题单独占整行；项目/父任务同行；状态/优先级同行置于左侧，创建时间只读展示于右侧。
- 时间信息单独成组，四列布局（计划开始 / 计划结束 / 截止日期 / 完成时间），统一带日历/时钟图标。
- 项目选择框：左侧文件夹图标，支持输入与选择，白底 Popover 下拉菜单。
- 父任务选择框：左侧层级图标，选项首行为"无（顶层任务）"，子任务缩进展示。
- 来源分段控件：带图标选项"手动创建"/"笔记"，选中态蓝色主题；选择"笔记"后右侧展示笔记 ID 输入框。
- 状态与优先级 Upgrade 为 40px 高度 Badge 按钮，与全局控件规范统一。
- 底部操作区固定，头尾浅色分割线规范，取消/保存按钮统一风格。
- 不同模式（新建/编辑/创建子任务）下标题、辅助说明、主按钮文案、父任务行为自动切换。

### 技术细节
- 新增 `buildComboboxSelect`、`buildComboboxOption`、`buildSegmentedControl` 等可复用 HTML 构建函数。
- 项目、父任务 Combobox 复用同一套 `initCombobox` 初始化逻辑。
- 状态/优先级 Badge + Popover 下拉菜单继续复用 3.1.0 的 `STATUS_BADGE_CONFIG` / `PRIORITY_BADGE_CONFIG` 配置体系。
- CSS 新增分区、行布局、分段控件、只读字段、日期包装、Combobox 等样式模块。
- 不影响现有任务创建、编辑、状态修改的数据链路。

### 兼容性说明
- 本次是弹窗 UI 层面的布局与交互优化，不涉及任务模型或 API 变更。
- 现有任务 CRUD 逻辑与数据持久化保持不变。

## [3.1.0] - 2026-05-16

### 发布说明
- 发布 `3.1.0`，对任务控制面板三个核心交互模块（状态下拉、优先级下拉、新建/编辑任务弹窗）进行统一视觉优化。
- 统一为"企业中台 + 轻政务 + 克制商务风"视觉语言，去掉原生 select 观感，改用自定义 Badge + Popover 下拉菜单。
- 新增"任务描述"字段，持久化到任务文档属性与元信息块，支持模板占位符 `{{description}}`。

### 功能增强
- 任务状态下拉菜单：Badge 风格按钮，按状态着色，带圆点与下拉箭头；菜单项包含状态色圆点、文字、勾选图标。
- 优先级下拉菜单：与状态统一交互风格，带旗帜图标，按优先级着色。
- 新建 / 编辑 / 创建子任务弹窗统一结构，根据模式切换标题与主按钮文案。
- 弹窗整体升级为宽屏卡片式布局（960px、16px 圆角、固定头尾、表单可滚动）。
- 表单控件统一为企业中台风格的 40px 高度输入框，focus 时蓝色边框 + 浅蓝外发光。
- 父任务字段在创建子任务时自动带入并展示轻提示。
- 两套下拉菜单均支持点击外部关闭、Esc 关闭、选中项高亮。

### 技术细节
- 状态与优先级配色集中维护在 `STATUS_BADGE_CONFIG` / `PRIORITY_BADGE_CONFIG`。
- 列表、表格、看板、时间轴视图中的状态下拉和优先级下拉均统一为同一套 Badge + Popover 组件。
- 不影响现有任务创建、编辑、状态修改的数据链路。

### 兼容性说明
- 本次不是任务模型重构，只有"任务描述"是明确新增字段。
- 现有任务 CRUD 逻辑保持不变。
- 新增属性 `custom-task-tracker-description` 写入任务文档，旧任务无此属性时显示为"无"。

## [2.2.1] - 2026-05-14

### 发布说明
- 发布 `2.2.1`，集中修复 `v2.2.0` 残留的日历小屏兼容、日历 more 交互、周报路径异常与重复导出内容污染问题。
- 本次继续保持“任务是真实思源文档”的主模型不变，只对日历承载与周报导出链路做定向纠偏。

### 缺陷修复
- 日历页默认改为每个日期最多显示 3 个事项，超出时在第 4 行显示 `more` 按钮，并支持展开当前日期的全部事项。
- 点击 `more` 后仅展开当前日期，但该日期所在整周整行会同步拉高；额外高度通过日历区域内部滚动承载，不再把整页布局撑坏。
- 日历页小屏场景下进一步压紧字号、间距与浮层尺寸，优先保证默认收起态整月日期完整显示。
- 已完成页导出的周报改为基于真实 hpath 定位到 `事项库/周报`，避免路径与标题混入异常 ID 片段。
- 周报重复导出时改为重建干净的 canonical 结构，只更新“本周工作事项”每日引用列表，并保留“本周工作总结”“下周工作计划”的正文。
- 旧版无编号标题周报与已被污染的周报，在再次导出时会被规范化修正，不再继续保留 front matter、重复 H1、脚注尾巴和误带入的任务正文内容。

### 兼容性说明
- 周报仍然不会参与任务索引与恢复语义。
- 对已存在的错误周报，本次优先在原文档上修正结构，不额外生成同周副本。

## [2.2.0] - 2026-05-14

### 发布说明
- 发布 `2.2.0`，补齐已完成周报导出链路，并统一时间字段语义与日历高密度展示承载。
- 本次继续保持“任务是真实思源文档”的主模型不变，只在周标题、周报、时间展示与日历布局上做定向增强。

### 功能改进
- 已完成页周标题升级为 `YYYY年WeekN(MM.DD~MM.DD)`，并按示例口径展示自然周编号。
- 已完成页每周分组新增“导出”按钮，可在 `事项库/周报` 下生成对应周报文档，重复导出只重建“本周工作事项”部分。
- 周报文档增加独立类型标记，并在重建索引时主动排除 `事项库/周报` 子树，避免周报被识别成任务。
- 表格页与已完成页的创建时间、计划时间、完成时间统一改为本地可读时间展示，不再直接暴露原始 UTC 串。
- dock、清单、日历卡片中的计划日期快捷编辑会保留原有时分，不再把已有计划时间静默改写为固定 `09:00`。
- 日历页按月份真实需要渲染 5 行或 6 行，并收紧单格任务区溢出与 `+N` 汇总承载。

### 兼容性说明
- 历史 `事项库/已完成` 周归档与更早的月归档目录都不会被迁移或删除。
- 历史任务若缺少可靠的 `createdAt` 属性，仍会保守回退到文档更新时间进行兼容展示。

## [2.1.0] - 2026-05-13

### 发布说明
- 发布 `2.1.0`，统一任务控制面板在时间轴、已完成、表格视图与整体视觉上的使用语义。
- 本次重点增强周归档语义、表格页页面级配置能力，以及任务面板的白色主视觉，不改动“任务是真实思源文档”的主模型。

### 功能改进
- 时间轴视图改为“未安排”单独展示，其余任务按计划时间倒序分组与排序，同一天内也会按计划时分倒序排列。
- 已完成视图改为按完成时间所在自然周分组展示，分组标题显示周一到周日的日期范围。
- 顶层任务完成后会归档到 `事项库/已完成/<周起始日>`，与已完成页的周分组语义保持一致。
- 表格页新增页面级“字段 / 排序”配置，可控制字段显示、字段顺序、当前排序和默认排序，并保留树形父子结构。
- 表格页保留状态可直接修改，优先级、计划、截止改为只读展示，并保留时间信息。
- 任务控制面板主要自绘区域统一为白色主视觉，保留边框、文字和交互层次。

### 兼容性说明
- 历史 `事项库/已完成/YYYY-MM` 月归档目录不会被批量迁移、删除或改写，旧任务仍可继续加载和展示。
- 旧版 `settings.json` 会自动兼容新的表格页配置结构，原有列宽设置继续保留。

## [2.0.0-hotfix] - 2026-05-10

### 发布说明
- 发布 `2.0.0-hotfix`，补修本地构建脚本拆分、Mac mini 本地同步缺文件，以及打包说明文档。
- 本次同时补充 `2.0.0` 应该明确写出的发布口径，避免 GitHub Release 说明过于简略。

### 功能改进
- 新增 `scripts/build_readme`，说明通用构建、本机构建和发布打包的用途与命令，便于另一台电脑排查打包问题。
- 将构建脚本拆分为通用 `scripts/build.mjs` 与本机专用 `scripts/build.local.mjs`。
- `npm run build:local` 现在会同步完整插件文件到本机思源目录，包含 `README` 和 `i18n`，避免本地插件目录缺文件导致插件无法正常使用。

### 缺陷修复
- 恢复 `scripts/build.mjs` 为纯净的发布构建脚本，不再混入 Mac mini 的本地目录路径。
- 修正 `2.0.0` 日志口径，使发布说明与实际版本、实际改动保持一致。

## [2.0.0] - 2026-05-10

### 发布说明
- 发布 `2.0.0`，重做多端同步恢复链路，并补上事项库重建任务索引入口。
- 本次继续保留 `tasks.json` 作为主索引文件，但启动阶段改为更保守地等待同步稳定，并在索引为空或明显缺失时自动尝试从事项库恢复。

### 功能改进
- 插件启动时会先等待思源同步状态稳定，再进入任务索引同步流程，避免在换机或刚启动时把“尚未同步到本地”的任务误判成已删除。
- 当 `tasks.json` 为空但事项库下已经存在任务文档时，启动阶段会自动扫描事项库并重建任务索引。
- 插件设置页“任务维护”新增“从事项库重建任务索引”入口，用于手动扫描事项库任务文档并重建 `tasks.json`。

### 缺陷修复
- 收紧启动期自动 `syncDeletedDocs` 的触发条件，只有在事项库已扫描到足够任务文档时才会执行破坏性清理，避免空索引被回写覆盖。
- 任务文档属性补写 `completedAt` 和 `sourceText`，为后续从事项库恢复任务索引提供更完整的数据基础。
- 构建脚本拆分为通用 `build.mjs` 和本机专用 `build.local.mjs`，前者用于 GitHub/发布包，后者用于 Mac mini 的本地插件同步。

## [1.9.0] - 2026-05-10

### 发布说明
- 发布 `1.9.0`，增强父任务完成归档路径与任务控制面板“已完成”页面的组织方式。
- 已完成父任务现在统一归档到事项库下的 `已完成 / YYYY-MM`，已完成页面则按创建月份分组展示并支持会话内展开状态保持。

### 功能改进
- 父任务完成后会自动归档到事项库下的 `已完成 / YYYY-MM`，若缺少“已完成”或月份节点会自动创建；子任务仍只随父任务一起归档。
- 任务控制面板“已完成”页面改为按任务创建月份分组，每个月份显示独立标题、数量和展开/折叠入口，最近两个月默认展开。
- 在当前打开的任务控制面板实例内，手动调整过的已完成月份展开状态在切换其他页面后返回仍会保持；关闭面板后重新打开则恢复默认展开规则。

## [1.8.5] - 2026-05-10

### 发布说明
- 发布 `1.8.5`，只修复删除任务后文档已删除但成功判定误报失败的问题。
- 本次不扩展新功能，不调整其他页面。

### 缺陷修复
- 删除任务文档时将“文档已不存在”视为删除阶段已完成，避免文档实际消失后任务记录仍被保留。
- 删除后校验改为短暂温和等待，不再因瞬时查询结果不稳定而中断 task record 清理。

## [1.8.4] - 2026-05-10

### 发布说明
- 发布 `1.8.4`，只修复 `1.8.x` 遗留的顶部入口行为和任务文档实际删除问题。
- 本次不扩展新功能，不调整其他页面。

### 缺陷修复
- 上方“任务追踪”按钮点击后直接打开任务控制面板，不再弹出新建任务、控制面板和插件设置菜单。
- 删除任务时先读取事项库中当前真实文档路径，调用删除接口后复查 `.sy` 文档是否实际消失，确认成功后才删除任务记录。
- 删除父任务时继续只删除顶层任务文档一次，并通过实际文档复查确保子任务、子子任务不残留孤儿文档。

## [1.8.3] - 2026-05-10

### 发布说明
- 发布 `1.8.3`，只修复 `1.8.2` 遗留的侧边控制面板快捷入口和任务文档删除问题。
- 本次不扩展新功能，不调整其他页面。

### 缺陷修复
- 侧边“任务控制面板”入口不再使用空壳 dock，改为直接打开现有任务控制面板，避免出现空白侧边栏。
- 删除任务时优先使用任务记录中的笔记本 ID 和文档路径删除对应任务文档，仅在路径缺失时回退读取块信息，保留父子任务级联删除。

## [1.8.2] - 2026-05-10

### 发布说明
- 发布 `1.8.2`，修复 `1.8.1` 中任务控制面板快捷入口、删除按钮图标样式和日历未安排区布局遗留问题。
- 本次只修正相关页面行为，不扩展新功能。

### 缺陷修复
- 移除任务控制面板的中间 dock 壳，侧边入口直接打开现有任务控制面板。
- 表格页和已完成页删除按钮统一改为垃圾箱图标，并复用现有轻量操作按钮样式。
- 日历页新增工具栏右侧“未安排任务”按钮，点击后以右侧悬浮侧栏显示未安排且未完成的任务，再次点击可隐藏，不再挤压日历主网格。

## [1.8.1] - 2026-05-10

### 发布说明
- 发布 `1.8.1`，聚焦任务追踪侧栏按钮精简、任务控制面板删除操作、日历未安排区布局重做和侧边快捷入口增强。
- 删除任务时会同步删除对应任务文档，并按任务子树级联处理子任务，避免遗留孤儿任务记录或文档。

### 功能改进
- 任务追踪侧栏移除“未安排 / 逾期 / 完成”筛选按钮，保留任务展示和核心筛选入口。
- 任务控制面板表格页操作列新增删除按钮，可删除任务记录、对应任务文档及其子任务。
- 已完成页新增“操作”列，提供删除已完成任务及归档父任务子树的能力。
- 日历页“未安排”模块从右侧栏改为底部横向任务带，日历主区域横向铺满页面，且未安排区不再显示已完成任务。
- 侧边新增“任务控制面板”快捷入口，直接复用现有控制面板打开逻辑。

## [1.8.0] - 2026-05-09

### 发布说明
- 新增任务“创建时间”字段，并发布 `1.8.0`。
- 本次把创建时间纳入新建任务、任务文档命名、任务控制面板表格页、已完成页和父任务完成归档流程。

### 功能改进
- 新建/编辑任务弹窗新增“创建时间”，默认使用当天日期；子任务同样记录自己的创建时间。
- 新建任务文档改为按创建时间生成 `MMDD-任务名` 文件名，任务标题仍保留用户输入的原始文本。
- 表格页和已完成页新增“创建时间”列，已完成页列顺序调整为任务、项目、来源、创建时间、完成时间。
- 父任务完成后会按创建时间月份移动到事项库下的 `YYYY-MM` 月归档文档；子任务单独完成时不移动，随父任务归档一起迁移。
- 任务文档元信息和自定义属性补充写入创建时间，归档后继续基于 `parentId` 保持已完成页父子关系。

## [1.7.6] - 2026-05-09

### 发布说明
- 修复任务控制面板“已完成”页列宽互相挤压的问题，并发布 `1.7.6`。
- 本次只调整已完成页表格总宽度计算，不改其他页面或交互。

### 缺陷修复
- 已完成页表格宽度现在按任务、项目、来源、完成时间四列宽度之和计算。
- 拖宽某一列时表格整体会随之变宽，不再压缩其他列；容器继续通过横向滚动承载超出宽度。

## [1.7.5] - 2026-05-09

### 发布说明
- 修复任务控制面板“已完成”页列宽配置未生效的问题，并发布 `1.7.5`。
- 本次只处理已完成页列宽状态、拖拽最小宽度和持久化，不调整其他页面。

### 缺陷修复
- 已完成页改用独立的列宽状态与 `completedTableColumnWidths` 持久化配置，不再被普通表格页的 `tableColumnWidths` 覆盖。
- 已完成页拖拽列宽时使用 `COMPLETED_TABLE_COLUMNS` 的专用最小宽度，项目、来源、完成时间等列现在可以继续拉窄。

## [1.7.4] - 2026-05-09

### 发布说明
- 继续微调任务控制面板“已完成”页面，并发布 `1.7.4`。
- 本次只调整已完成页任务、项目、来源、完成时间四列的列宽配置，不改其他页面或交互。

### 缺陷修复
- 将已完成页四列默认宽度与最小宽度按当前配置减半，让列宽拖拽和初始布局更紧凑。

## [1.7.3] - 2026-05-09

### 发布说明
- 继续修正任务控制面板“已完成”功能，并发布 `1.7.3`。
- 本次只修复完成任务后的数据写回链路，以及已完成页专用列宽的最小宽度。
- 完成任务时只变更状态、完成时间和更新时间，保留原任务的父任务、项目和来源信息。

### 缺陷修复
- 修复完成任务时 patch 规范化会把未传入字段写成空值的问题，避免 `parentId`、`project`、`sourceBlockId`、`sourceDocId`、`sourceText` 被覆盖丢失。
- 完成任务后同步任务文档时，来源、父任务、项目元信息会继续使用原任务字段，不再被改成“手动创建 / 无 / 未设置”。
- 收紧已完成页任务、项目、来源、完成时间列的最小宽度，让列宽拖拽更灵活，同时不影响普通表格页。

## [1.7.2] - 2026-05-09

### 发布说明
- 继续修正任务控制面板“已完成”页面，并发布 `1.7.2`。
- 本次只收口 completed table 自身的父子树、项目/来源渲染、交互规则和文字颜色，不调整其他页面。
- 已完成页保留表格外观，但使用独立的 completed view 渲染规则，不再直接套用普通表格页的项目/来源单元格行为。

### 缺陷修复
- 已完成页改为基于 completed 任务集合单独建树：父任务与子任务都 completed 时恢复层级，父任务未 completed 时子任务保持顶层。
- 已完成页项目列和手动创建来源改为普通文本，仅来源为笔记时保留点击跳转。
- 调整已完成页 completed 行与普通文本颜色，避免整页文字因完成态透明度而偏浅。

## [1.7.1] - 2026-05-09

### 发布说明
- 修复 `1.7.0` 引入的 completed 渲染回归，并发布 `1.7.1`。
- 本次只处理“任务进入 completed 状态后，日历页和已完成页打不开”的问题，不扩展其他功能或样式。
- 重点收口 completed 任务进入任务控制面板渲染链时的兼容性，保持既有 completed 视图口径不变。

### 缺陷修复
- 修复存量 completed 任务缺少标题时，切换到“日历”或“已完成”页会触发渲染异常的问题。
- 为任务存储加载与写入补上标题归一化：缺失标题时回退到文档路径名，避免 completed 任务进入日历页与已完成页后再次打断渲染。
- 保持表格 / 清单 / 时间轴 / 看板继续不显示 completed 任务，并保持看板不出现“已完成”列。

## [1.7.0] - 2026-05-09

### 发布说明
- 在任务控制面板（TaskManagerTab）新增独立的“已完成”页签，并发布 `1.7.0`。
- 这次把活动任务视图、已完成任务视图和日历视图的数据口径彻底拆开：表格 / 清单 / 时间轴 / 看板只显示未完成任务，日历继续显示全部任务，而所有 completed 任务统一归集到新的“已完成”页。
- 同时统一 completedAt 的写入与清空规则，并让已完成页按“已完成任务集合”重新构建父子树。

### 功能改进
- 新增“已完成”页签，位置在“日历”右边，并复用表格样式展示任务、项目、来源、完成时间四列。
- 已完成页支持搜索，并按已完成任务集合保留父子关系：子任务先完成时可单独顶层显示，父任务后续完成后会自动恢复层级。
- 看板页面移除“已完成”列，活动视图只保留未完成任务。

### 缺陷修复
- 统一 completedAt 生命周期：任务首次变为 completed 时写入，改回非 completed 时清空，再次完成时重新写入。
- 修正状态选择器与“完成任务 / 重新打开”按钮的完成时间行为，让两种入口都走同一套状态迁移规则。

## [1.6.9] - 2026-05-08

### 发布说明
- 继续修正任务控制面板（TaskManagerTab）表格视图，并发布 `1.6.9`。
- 这次只处理表格里“项目”列和“来源”列中的普通文本，让它们与“任务”列文本在元素类型和文本样式来源上完全一致。
- 目标是消除项目列与“手动创建”等普通来源文本仍然偏黑、偏粗的问题，同时不影响已经正常的可点击来源文本。

### 缺陷修复
- 将表格“项目”列与“来源”列普通文本切到与“任务”列相同的 button 文本基类，并保留其不可交互表现。
- 保持来源为笔记时的可点击文本逻辑不变，不恢复框框、底色或胶囊视觉。

## [1.6.8] - 2026-05-08

### 发布说明
- 继续修正任务控制面板（TaskManagerTab）表格视图，并发布 `1.6.8`。
- 这次直接让表格“项目”“来源”两列复用“任务”列标题的同一套文本类，避免再出现局部样式近似但视觉仍偏粗、偏黑的问题。
- 目标是让项目、来源两列在字体、字号、字重、颜色、行高上都和任务列真正一致，同时保持既有点击行为不变。

### 缺陷修复
- 将表格“项目”“来源”两列直接切到与“任务”列相同的文本样式来源，不再单独维持一套近似样式。
- 保持来源为笔记时仍可点击、手动创建仍不可点击，不恢复框框、底色或胶囊视觉。

## [1.6.7] - 2026-05-08

### 发布说明
- 继续修正任务控制面板（TaskManagerTab）表格视图，并发布 `1.6.7`。
- 这次只收口表格“项目”“来源”两列在表格里的文本渲染方式，让它们真正表现为与“任务”列一致的普通表格文本。
- 目标是消除项目、来源两列仍然偏黑、偏粗的问题，同时保持来源点击跳转与手动创建的既有行为不变。

### 缺陷修复
- 将表格“项目”“来源”两列从表格内的 pill/button 字体残留样式中剥离，改为表格局部普通文本样式。
- 保持来源为笔记时仍可点击、手动创建仍不可点击，不恢复框框、底色或胶囊视觉。

## [1.6.6] - 2026-05-08

### 发布说明
- 继续修正任务控制面板（TaskManagerTab）表格视图，并发布 `1.6.6`。
- 这次只收口表格“项目”“来源”两列的文本样式，让它们与任务列中的任务标题保持一致。
- 目标是消除项目、来源两列相对任务名偏小、偏浅的问题，同时保留来源可点击的交互。

### 缺陷修复
- 调整表格“项目”“来源”两列的字体、字号、字重与默认颜色，使其与任务列任务名保持一致。
- 保持来源为笔记时的点击跳转逻辑不变，仅将强调色改为 hover 时再显示。

## [1.6.5] - 2026-05-08

### 发布说明
- 继续修正任务控制面板（TaskManagerTab）表格视图与日历页面顶部月份导航，并发布 `1.6.5`。
- 这次在表格“来源”列收口来源于笔记时的视觉样式，只保留蓝色可点击差异；同时为表格操作列补上编辑入口，并复用同一套任务弹窗进入编辑模式。
- 另外修正日历月份标题左侧 chevron 的朝向，让它明确表示向前翻月。

### 功能改进
- 表格视图“操作”列新增编辑按钮，点击后打开预填充当前任务信息的编辑弹窗，保存时更新现有任务而不是新建任务。
- TaskDialog 扩展为创建 / 编辑双模式，编辑模式会回填当前任务的标题、项目、父任务、状态、优先级、来源、计划与截止信息。

### 缺陷修复
- 表格“来源”列中，来源于笔记时去掉额外框框和底色，并统一为与“手动创建”一致的字号、字重、行高，只保留蓝色可点击差异。
- 日历页面月份标题左侧按钮箭头方向修正为朝左，并保持现有轻量 chevron 风格。

## [1.6.4] - 2026-05-08

### 发布说明
- 继续修正任务控制面板（TaskManagerTab）时间轴、看板与日历右侧“未安排”卡片第二行来源胶囊的尺寸一致性，并发布 `1.6.4`。
- 这次只收口来源胶囊的文字节点与按钮默认视觉，不改项目、状态、优先级、计划、截止胶囊样式。
- 目标是让来源胶囊只保留颜色与可点击性的差异，不再出现尺寸和字号偏大的问题。

### 缺陷修复
- 修正手动创建与来源于笔记两种来源胶囊在第二行里都偏大、偏松的问题，使其高度、内边距、字号、行高与旁边胶囊一致。
- 保持手动创建灰色不可点击、来源于笔记可点击跳转的既有交互逻辑不变。

## [1.6.3] - 2026-05-08

### 发布说明
- 继续修正任务控制面板（TaskManagerTab）时间轴、看板与日历右侧“未安排”卡片第二行胶囊的视觉一致性，并发布 `1.6.3`。
- 这次只收口项目胶囊与来源胶囊的文字颜色、尺寸与按钮默认样式，不改交互逻辑与其他视图。
- 目标是让项目、来源、状态、优先级、计划、截止六类胶囊在同一行里保持统一视觉。

### 缺陷修复
- 修正来源于笔记时的来源胶囊尺寸偏大、字体偏大的问题，使其与同一行其他胶囊保持一致的高度、内边距与字号。
- 修正项目胶囊文字颜色偏黑的问题，使其与同一行其他胶囊保持一致，不再显得更重。
- 保持来源于笔记可点击跳转、手动创建不可点击的既有行为不变。

## [1.6.2] - 2026-05-08

### 发布说明
- 继续修正任务控制面板（TaskManagerTab）时间轴、看板与日历视图右侧“未安排”列，并发布 `1.6.2`。
- 这次把任务卡片两行内容重新分布：第一行只保留任务名与轻量操作，第二行统一收口为项目、来源、状态、优先级、计划、截止胶囊。
- 同时将日历月份切换按钮改为轻量 chevron 风格，并为右侧“未安排”列加入拖动调宽能力。

### 缺陷修复
- 时间轴 / 看板 / 日历右侧未安排卡片第一行改为“任务名 + 添加子任务 + 完成任务 / 重新打开”，按钮左对齐并紧跟标题。
- 项目、来源从第一行移到第二行，并改成与侧边栏风格接近的胶囊；来源于笔记仍可点击跳转，手动创建仍保持灰色不可点击。
- 日历页月份标题左右按钮去掉圆形按钮样式，改为更接近任务展开 / 折叠的轻量 chevron 按钮。
- 日历页右侧“未安排”列支持拖动调整宽度，并在当前 tab 即时生效且持久化保存。

## [1.6.1] - 2026-05-08

### 发布说明
- 继续修正任务控制面板（TaskManagerTab）时间轴、看板与日历右侧未安排卡片的两行结构，并发布 `1.6.1`。
- 这次改为侧边栏风格的胶囊交互：第一行所有附属元素紧跟任务名左对齐，第二行使用可点击修改的状态 / 优先级 / 计划 / 截止胶囊。
- 本轮只调整这三个视图，不再改清单视图、表格视图、任务追踪侧边栏和插件设置页。

### 缺陷修复
- 时间轴 / 看板 / 日历右侧未安排卡片改为真正两行：第一行“任务名 + 项目胶囊 + 来源胶囊 + 添加子任务 + 完成任务/重新打开”，第二行“状态 / 优先级 / 计划 / 截止”胶囊。
- 第二行不再使用一排 `select` / `input` 控件条，而是改为侧边栏风格的胶囊包裹交互控件，并保留点击选日期与点击选择的能力。
- 保持时间轴中的“未安排”分组继续位于最上方，同时移除父任务属性与旧的第三、第四行 controls。

## [1.6.0] - 2026-05-08

### 发布说明
- 为任务控制面板（TaskManagerTab）的表格、时间轴、看板与日历右侧未安排区域补齐一组交互增强，并发布 `1.6.0`。
- 表格视图补上来源列、可见轻量操作按钮与列宽拖拽；时间轴 / 看板 / 日历右侧未安排卡片统一收敛成两行直接编辑结构。
- 本轮不改任务追踪侧边栏、插件设置页、顶部插件菜单，也不再动已经阶段性稳定的清单视图结构。

### 功能改进
- 表格视图删除“父任务”“子任务”列，新增来源列；来源展示与来源跳转逻辑直接复用清单视图规则。
- 表格视图引入 `<colgroup>` 列宽管理与表头拖拽调宽能力，列宽会即时生效并持久化到插件设置中。
- 时间轴中的“未安排”分组移动到最上方；时间轴 / 看板 / 日历右侧未安排卡片统一为“两行 + 第二行直接编辑状态/优先级/计划/截止”的紧凑结构。

## [1.5.0] - 2026-05-08

### 发布说明
- 完成任务控制面板（TaskManagerTab）清单视图本轮布局调整，并发布 `1.5.0`。
- 将第二行 controls 的有效区域最终收口为稳定的清单布局实现，保留标题首字左对齐，同时让第二行宽度与层级结构表现更稳定。
- 本次作为清单视图阶段性完成版本，对外发布整理后的最终效果。

### 功能改进
- 清单视图任务项改为使用更稳定的两列网格骨架，让第一行主体与第二行 controls 共用明确的起始列关系。
- 第二行 controls 使用固定上限宽度并直接挂在内容列中，避免前几轮多套右边界计算方式互相干扰。
- 清单视图子项与 children 区域继续保持跨列铺开，状态、优先级、计划、截止四个字段继续等宽，不影响其他视图。

## [1.4.9-hotfix2] - 2026-05-08

### 发布说明
- 继续修正任务控制面板（TaskManagerTab）清单视图第二行的有效区域计算，并发布 `1.4.9-hotfix2`。
- 这次不再使用 `width / calc / max-width` 控制第二行宽度，改为直接用左边距和右边距限定 controls 区域。
- 本轮仍然只调整第二行，不改动第一行标题、胶囊、任务数和按钮位置。

### 缺陷修复
- 清单视图第二行 controls 改为保留 `margin-left: var(--task-title-offset)`，并新增 `margin-right: 24%` 直接收右边界。
- 删除第二行 controls 上的 `width`、`max-width`、`padding-right` 以及 control grid 上的 `margin-left: auto`，避免多套右对齐逻辑互相打架。
- 状态、优先级、计划、截止四个字段继续保持 `repeat(4, minmax(0, 1fr))` 等宽自适应列，不影响第一行与其他视图。

## [1.4.9-hotfix] - 2026-05-08

### 发布说明
- 修正任务控制面板（TaskManagerTab）清单视图第二行 controls 在 `1.4.9` 中右边界仍未生效的问题，并发布 `1.4.9-hotfix`。
- 保留当前已经正确的左对齐起点，只重新计算第二行的有效宽度，让右边界稳定落在父任务框约四分之三区域。
- 本轮仍然只调整第二行，不改动第一行标题、胶囊、任务数和按钮位置。

### 缺陷修复
- 清单视图第二行 controls 改为基于“最大宽度减左偏移”计算实际宽度，避免 `max-width` 直接作用在整块时把右边界提前收窄。
- 第二行继续保留 `margin-left: var(--task-title-offset)` 左起点与 `margin-left: auto` 的四等宽 control grid，对齐标题首字的同时恢复右侧目标边界。
- 状态、优先级、计划、截止四个字段继续保持 `repeat(4, minmax(0, 1fr))` 等宽自适应列，不影响第一行与其他视图。

## [1.4.9] - 2026-05-08

### 发布说明
- 继续微调任务控制面板（TaskManagerTab）清单视图第二行 controls 的右侧边界，并发布 `1.4.9`。
- 保留上一版已经正确的左对齐起点与四等宽自适应列，仅限制第二行最大宽度，避免继续向右铺得过满。
- 本轮只调整第二行右边界，不改动第一行标题、胶囊、任务数和按钮位置。

### 功能改进
- 清单视图第二行继续从标题起点开始计算宽度，同时新增最大宽度限制，避免 controls 延伸到父任务卡片过右区域。
- 第二行 controls 继续保留固定 `12px` 右留白，并通过 `margin-left: auto` 让四个设置项在限定宽度内稳定贴齐右边界。
- 状态、优先级、计划、截止四个字段继续保持 `repeat(4, minmax(0, 1fr))` 等宽自适应列，不回退到旧的固定宽度方案。

## [1.4.8] - 2026-05-08

### 发布说明
- 继续修正任务控制面板（TaskManagerTab）清单视图第二行 controls 的横向铺开方式，并发布 `1.4.8`。
- 去掉第二行把四个设置项收缩在右侧小块区域内的布局逻辑，改为让 controls 从标题起点开始占满整块有效区域。
- 本轮只调整第二行，不改动第一行标题、胶囊、任务数和按钮位置。

### 功能改进
- 清单视图第二行移除 `display: flex`、`justify-content: flex-end` 与固定 `136px` 四列，改为在整块可用宽度内使用四个等宽自适应列。
- 第二行 controls 继续从标题起点开始计算宽度，并保留固定 `12px` 右留白，四个设置项不再缩在右边一小块。
- 第二行里的状态、优先级、计划、截止四个字段继续等宽，并统一使用 `width: 100%` 填满各自列。

## [1.4.4] - 2026-05-08

### 发布说明
- 继续微调任务控制面板（TaskManagerTab）清单视图第一行与第二行的横向对齐关系，并发布 `1.4.5`。
- 修正第二行设置栏过度左缩进的问题，改回以标题起点为左边界、以固定右侧留白为终点的稳定布局范围。
- 让第一行操作按钮真正紧跟在标题、胶囊和任务数后面，不再被 summary 挤到最右侧。

### 功能改进
- 清单视图第二行继续保持右对齐，但其可用宽度改为“标题起点到卡片右侧固定留白”的范围，不再使用大百分比右侧留白。
- 清单视图第一行 summary 改为局部宽度控制，标题/胶囊/任务数/操作按钮现在连续排布在左侧，同时继续保留标题截断能力。
- 状态、优先级、计划、截止四个设置项继续等宽，tooltip 与轻量按钮样式保持不变。

## [1.4.4] - 2026-05-08

### 发布说明
- 继续微调任务控制面板（TaskManagerTab）清单视图的行内操作与第二行设置栏布局，并发布 `1.4.4`。
- 将“添加子任务 / 完成任务 / 重新打开”按钮前移到第一行末尾，并恢复与侧边栏一致的 tooltip 触发方式。
- 让第二行只保留四个设置项，并重新调整为右对齐但保留明显右侧留白。

### 功能改进
- 清单视图第一行改为“toggle + 标题/胶囊/任务数 + 轻量 actions”，操作按钮不再占用第二行空间。
- 清单视图第二行仅保留状态、优先级、计划、截止四个等宽设置项，并整体右对齐到父任务卡片右侧附近，同时避免贴边。
- 第一行自定义 action button 补齐 `ariaLabel`、`aria-label` 与 `data-position`，修复“添加子任务”“完成任务 / 重新打开”的悬浮提示不生效问题。

## [1.4.3] - 2026-05-08

### 发布说明
- 继续优化任务控制面板（TaskManagerTab）清单视图的第二行设置栏布局，并发布 `1.4.3`。
- 将第二行设置项改为沿标题文本起点左对齐，保持同层级任务在不同父节点下也能稳定对齐。
- 收窄末尾两个操作按钮的视觉重量，同时保留稳定可见性与明确提示。

### 功能改进
- 清单视图第二行 settings 改为与第一行标题首字共用起始偏移，不再整体右对齐到卡片右边。
- 状态、优先级、计划、截止四个设置项继续保持等宽，并略微加宽以显示更完整的日期内容。
- “添加子任务”“完成任务/重新打开”按钮改为更轻量的无边框样式，尺寸与 hover 强度更接近展开/收起按钮。

## [1.4.2-hotfix] - 2026-05-08

### 发布说明
- 延续 `1.4.2` 的同一轮问题修正，继续处理任务控制面板（TaskManagerTab）清单视图第二行末尾 action 按钮的可见性问题。
- 不再依赖 SiYuan 内置 `block__icon ariaLabel`，改为插件自有 action button 样式，避免按钮继续被宿主样式挤压或覆盖。
- 保持四个设置项等宽与第二行右对齐结构，同时补强窄宽度下的横向稳定性。

### 缺陷修复
- 清单视图末尾“创建子任务”“完成任务/重新打开”按钮改为插件自定义按钮，独立定义尺寸、边框、背景、hover 与 SVG 颜色。
- 清单视图第二行继续保持“control-grid + actions”分组，actions 区不再复用全局 icon button 样式。
- `@media (max-width: 820px)` 下为清单视图 controls 增加横向滚动保护，避免窄宽度时 actions 再次被压缩消失。

## [1.4.2] - 2026-05-08

### 发布说明
- 继续修复任务控制面板（TaskManagerTab）清单视图第二行 controls 的布局问题，并发布 `1.4.2`。
- 将四个设置项与末尾 actions 按钮拆成独立控制组，避免继续互相挤压。
- 处理窄宽度下媒体查询对清单视图布局的覆盖问题。

### 缺陷修复
- 清单视图第二行改为“control-grid + actions”双分组结构，修复“创建子任务”“完成任务/重新打开”按钮不可见的问题。
- 清单视图第二行整体右对齐到任务卡片右侧，并与边框保持稳定留白。
- 状态、优先级、计划、截止四个设置项在清单视图中保持统一宽度，窄宽度下也不会再被全局媒体查询打散。

## [1.4.1] - 2026-05-08

### 发布说明
- 修复任务控制面板（TaskManagerTab）清单视图中的标题行与 controls 布局问题，并发布 `1.4.1`。
- 让第二行末尾的操作按钮稳定可见，并统一四个编辑控件宽度。
- 调整标题后的项目、来源、子任务数排列方式，改为紧跟标题靠左展示。

### 缺陷修复
- 清单视图第二行改为等宽四列控件加独立 actions 列，修复“创建子任务”“完成任务/重新打开”按钮被挤到边缘的问题。
- 清单视图第一行增加局部 summary 分组，项目、来源、子任务数不再整体被标题推到最右侧。
- 状态、优先级、计划、截止四个控件在清单视图中统一宽度，同时继续保持紧凑高度。

## [1.4.0] - 2026-05-08

### 发布说明
- 重构任务控制面板（TaskManagerTab）的清单视图层级展示，并发布 `1.4.0`。
- 仅保留顶层父任务卡片框，子任务改为更轻量的层级条目。
- 在清单视图中新增来源笔记胶囊跳转，并修复末尾操作按钮不可见问题。

### 功能改进
- 清单视图第一行改为展示展开按钮、任务标题、项目胶囊、来源胶囊与子任务数，不再重复显示只读状态/优先级/日期信息。
- 来源为手动创建时显示灰色“手动创建”胶囊；来源于笔记时显示可点击的蓝色来源胶囊，点击后打开来源笔记。
- 清单视图中的“创建子任务”“完成任务/重新打开”按钮恢复稳定可见，同时继续保持紧凑布局。

### 界面调整
- 仅顶层父任务保留大卡片框，子任务、子子任务与更深层级去框并同步收紧缩进。
- 子层级继续保留层级引导与展开收起交互，不影响表格、时间轴、看板与日历视图布局。

## [1.3.0] - 2026-05-08

### 发布说明
- 统一插件用户入口与命名，发布 `1.3.0`。
- 将“任务管理器”统一更名为“任务控制面板”，并收口顶部菜单与侧栏入口。
- 删除独立任务日历的用户可见入口，仅保留任务控制面板内的日历视图。

### 功能改进
- TaskDock 头部按钮改为“新建任务 / 打开任务控制面板 / 清理已删除文档”，不再单独提供任务日历按钮。
- 顶部插件菜单收敛为“新建任务 / 打开任务控制面板 / 插件设置”，减少分散入口。
- 插件设置页保留事项库绑定能力，新增“清理已删除任务记录”维护入口，并同步更新使用帮助文案。
- 任务控制面板搜索框 placeholder 调整为“搜索任务、项目等”，同时继续保留已有中文输入法修复。

### 工程调整
- 移除独立 `CalendarTab` 及其命令、注册与 i18n 残留文案，避免继续维护无用户入口页面。

## [1.2.6] - 2026-05-07

### 发布说明
- 修复任务追踪侧栏（TaskDock）的标题与日期胶囊细节样式，并发布 `1.2.6`。
- 修复任务控制面板（TaskManagerTab）的搜索输入与展开按钮样式问题。
- 收紧任务管理器搜索框宽度，避免继续挤占工具栏空间。

### 缺陷修复
- TaskDock 中任务名与状态胶囊之间的间距略微拉开，计划时间与“未设置”胶囊的左右留白更对称，并整理了重复的日期 chip 样式定义。
- TaskManagerTab 表格/清单视图里的展开按钮去掉蓝色圆形外圈，仅保留稳定点击区域与 chevron 箭头。
- TaskManagerTab 搜索栏新增 composition 处理，中文拼音输入法不再被即时重渲染打断，同时搜索框宽度缩减到更紧凑的范围。

## [1.2.5] - 2026-05-07

### 发布说明
- 继续微调任务追踪侧栏（TaskDock）的布局与展示规则，发布 `1.2.5`。
- 删除每个任务右侧的操作按钮组，保留标题打开、展开收起与元信息编辑交互。
- 让标题与状态/计划/截止胶囊保持固定小间距并整体左对齐。

### 缺陷修复
- 每个任务右侧的打开、子任务、完成/重开、移除按钮已全部删除，不再占用侧栏宽度。
- 状态、计划、截止胶囊改为紧跟在任务名后面，拉宽侧栏时不再整体向右漂移。
- 子任务缩进与当前日期/状态展示规则保持不变，继续保留标题点击、展开收起与日期点选能力。

## [1.2.4] - 2026-05-07

### 发布说明
- 继续优化任务追踪侧栏（TaskDock）的排版密度与层级缩进，发布 `1.2.4`。
- 将任务标题与状态/计划/截止胶囊收拢到同一排，提升侧栏信息密度。
- 收紧子任务缩进与 children 引导线间距，减少横向空间浪费。

### 缺陷修复
- 子任务、子子任务缩进明显减小，展开按钮左边沿更贴近父级竖线。
- 任务标题与状态、计划、截止胶囊合并为单行展示，不再分成上下两行。
- 状态与日期胶囊进一步收紧，计划/截止左右留白更对称，同时继续保留点击编辑交互。

## [1.2.3] - 2026-05-07

### 发布说明
- 修复任务追踪侧栏（TaskDock）中的日期选择交互问题，发布 `1.2.3`。
- 调整侧栏元信息展示规则，继续保持当前紧凑布局。
- 本次仅修复 TaskDock，不涉及任务管理器界面与样式。

### 缺陷修复
- 计划、截止日期 chip 点击后会主动触发内部日期选择器，修复无法稳定设置日期的问题。
- 侧栏元信息改为仅展示状态、计划，以及有值时展示截止，不再显示优先级。
- 日期 chip 继续保持紧凑尺寸，不引入更大的输入框或破坏标题行布局。

## [1.2.2] - 2026-05-07

### 发布说明
- 调整任务追踪侧栏（TaskDock）的样式与交互细节，发布 `1.2.2`。
- 去掉展开/收起按钮的圆形外圈，保留 chevron 方向反馈与稳定点击区域。
- 顶层任务继续保留卡片框体，子任务改为更轻量的层级展示。

### 缺陷修复
- 任务追踪侧栏的展开/收起按钮不再显示圆形边框与背景，但悬停反馈和点击区域保持稳定。
- 任务元信息只显示状态、优先级、计划、截止的值，不再重复显示字段名前缀。
- 计划/截止日期 chip 保持当前紧凑尺寸，同时可直接点击设置日期。
- 子任务与子子任务去掉独立框体，仅保留缩进与层级引导线，减少多层嵌套的视觉噪音。

## [1.2.1] - 2026-05-07

### 发布说明
- 修复 `1.2.0` 中任务管理器界面被连带改动的问题，发布 `1.2.1`。
- 恢复任务管理器到 `v1.1.0` 的界面风格，同时保留 TaskDock 当前边栏优化结果。
- 拆开任务管理器与任务追踪边栏的主体样式，避免后续相互影响。

### 缺陷修复
- 任务管理器的工具栏、表格、清单、时间轴、看板与日历布局恢复为 `v1.1.0` 风格。
- 任务追踪边栏继续保留 `1.2.0` 的紧凑展示与可编辑元信息，不再被任务管理器样式回退影响。
- 任务管理器与任务追踪边栏的父子任务展开按钮都保留 chevron 风格，但主体样式已解耦。

## [1.2.0] - 2026-05-07

### 发布说明
- 基于 `1.1.0` 继续打磨侧边栏交互，发布 `1.2.0`。
- TaskDock 元信息进一步收敛为可直接编辑的紧凑四项：状态、优先级、计划、截止。
- 新建任务“来源”交互收窄为 `手动创建` / `笔记` 两种模式，并统一任务树的展开/收起箭头样式。

### 功能改进
- TaskDock 不再显示项目与父任务，计划/截止改为 `MM-DD` 展示，并支持在侧边栏直接修改状态、优先级、计划和截止。
- 新建任务时，来源选择只保留“手动创建”与“笔记”两种模式；选择“笔记”后仅需填写一个笔记 ID。
- Dock 与任务管理器中的父子任务展开按钮统一为固定尺寸的 chevron 风格，避免展开/收起时标题行抖动。

### 工程调整
- 删除任务来源搜索相关代码与样式，移除未再使用的文档搜索 API。

## [1.1.0] - 2026-05-07

### 发布说明
- 修复顶部“任务追踪 -> 插件设置”入口，改为打开本插件自己的设置面板。
- 调整本地构建与发布流程：保留本地 `release/` 机制，但停止将 `release/`、`dist/`、`node_modules/` 和 `.DS_Store` 纳入 Git 跟踪。
- `npm run build` 成功后，自动将 `dist/` 发布产物同步到本地思源插件目录。

### 功能改进
- 重做侧边栏 TaskDock，改为更高密度的任务列表布局，保留高频操作并移除常驻详细编辑控件。
- 新建任务时支持通过“搜索笔记”或“填写笔记 ID”指定来源笔记。
- 来源为整篇笔记时，会在任务文档中继续显示为有效引用，而不是退回“手动创建”。

### 工程调整
- `.gitignore` 新增 `release/`、`dist/`、`node_modules/`、`**/.DS_Store`。
- 本地 `npm run release` 仍会生成 `release/siyuan-plugin-task-tracker-v1.1.0.zip`，仅不再推送这些产物到 GitHub 仓库。

## [1.0.0] - 2026-05-07

### 发布说明
- 将 GitHub 作为后续版本管理与发布的统一基线。
- 删除误回退版本 `0.4.0` 的发布基线影响，正式以 `1.0.0` 作为首个 GitHub 发布版本。
- 产出并保留 `v1.0.0` 的插件发布包，供后续 Releases 使用。

### 当前能力
- 支持将指定文档设置为事项库。
- 支持从顶栏、当前文档、选中块创建任务文档。
- 支持记录状态、优先级、项目、计划时间、截止日期、父任务和来源块。
- 支持通过停靠栏、任务管理器和日历视图追踪任务。
- 支持将任务索引数据保存在 `tasks.json` / `settings.json`，并同步任务元数据到文档属性。
